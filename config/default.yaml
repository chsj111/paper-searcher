arxiv:
  categories:
    - cs.AI
  max_results_per_query: 500

llm:
  api_url: "https://api.cursorai.art/v1/chat/completions"
  api_key: ""  # 通过环境变量 PAPER_SEARCHER_LLM_API_KEY 设置
  model: "gpt-5.4"
  temperature: 0.3
  max_workers: 8
  retries: 3
  timeout: 120
  batch_size: 5

filter:
  queries: []

storage:
  data_dir: "data"
